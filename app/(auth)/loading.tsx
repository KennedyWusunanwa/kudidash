import { RouteLoading } from "@/components/motion/route-loading";

export default function Loading() {
  return <RouteLoading title="Loading authentication" subtitle="Preparing sign-in and account access..." />;
}
